apiVersion: apps/v1
kind: Deployment
metadata:
    name: marketplace-bot
spec:
    template:
        spec:
            containers:
                - name: bot
                  resources:
                      requests:
                          memory: "256Mi"
                          cpu: "250m"
                      limits:
                          memory: "512Mi"
                          cpu: "400m"
